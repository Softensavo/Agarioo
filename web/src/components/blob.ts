export default class Blob {
    pos: { x: number, y: number }; 
    radius: number;
    
    constructor(pos, radius) {
        this.pos = pos; 
        this.radius = radius;
        
 
            
        }
    }

